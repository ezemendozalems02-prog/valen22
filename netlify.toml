# El proyecto ahora es una app Next.js (venta de entradas con Mercado Pago).
# El deploy por drag & drop YA NO sirve: hay que conectar el repositorio Git
# (Netlify detecta Next.js y usa su runtime) o, preferentemente, deployar en
# Vercel. Ver MERCADOPAGO_SETUP.md.

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
